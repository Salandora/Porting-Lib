moduleDependencies(project, [
        "tool_actions"
])

mixinExtras(dependencies)

fabricModule(project, ["fabric-resource-loader-v0"])

processResources {
    duplicatesStrategy = DuplicatesStrategy.WARN
}