moduleDependencies(project, [
        "tool_actions"
])

mixinExtras(dependencies)

fabricModule(project, [
        "fabric-loot-api-v2",
        "fabric-resource-loader-v0",
        "fabric-registry-sync-v0"
])

processResources {
    duplicatesStrategy = DuplicatesStrategy.WARN
}